# WinCell

See the [project README](../README.md) for features, build instructions, and local media setup.

Run `bash build.sh` from this directory to build the native macOS app. Media and generated app bundles are excluded from the code repository.
